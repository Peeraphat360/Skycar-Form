# ข้อมูลส่วนตัวลูกค้า (PII) — ห้าม commit ขึ้น public repo
backend/data/
backend/backups/

# Local editor / Claude settings
.claude/launch.json

# Env / secrets
*.env
.env

# Dependencies — ไม่ควร commit (ทำ repo บวม + เสี่ยงรั่ว)
node_modules/

# Compiled output — artifact ไม่ควรอยู่ใน repo
dist/
backend/dist/
